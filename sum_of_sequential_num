#include <stdio.h>
//sum of numbers from n to 1

int sum(int n)
{
    if(n==1)
    {
        return 1;
    }
    else
        return n+sum(n-1);
}

int main(void)
{
    int num;
    printf("Enter number: ");
    scanf("%d",&num);
    printf("The result is %d",sum(num));

    return 0;

}
